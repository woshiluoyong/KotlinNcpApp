<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stephen.kotlin.demo">
    <!-- 允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.view.act.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp2.MvpActivity" />
        <activity android:name=".mvp.view.act.TestAnkoActivity"></activity>
        <!--
        <provider
            android:name="cn.bmob.v3.util.BmobContentProvider"
            android:authorities=".BmobContentProvider">
        </provider>
        -->
        <meta-data
            android:name="PGYER_APPID"
            android:value="6024fdb5b544c0d71c7db0f602e5f439"></meta-data>
    </application>

</manifest>